{{- $architecture := or .architecture "arm64" -}}
{{- $image := or .image "ubuntu-touch-pinetab.img" -}}
{{- $variant := or .variant "mali" -}}

architecture: {{ $architecture }}
actions:
  - action: recipe
    description: Pine64 common
    recipe: pine64-common.yaml
    varables:
      architecture: {{ $architecture }}
      variant: {{ $variant }}

  - action: run
    description: Set boot-pinetab to default
    chroot: true
    command: cp -av /boot/boot-pinetab.scr /boot/boot.scr

  - action: filesystem-deploy
    description: Deploying filesystem into image

  - action: raw
    description: Installing bootloader
    origin: u-boot
    source: u-boot-sunxi-with-spl-sopine.bin
    offset: 8192

  - action: run
    description: Create bmap file
    postprocess: true
    command: bmaptool create {{ $image }} > {{ $image }}.bmap

  - action: run
    description: Compressing image
    postprocess: true
    command: pigz -f9 {{ $image }}
